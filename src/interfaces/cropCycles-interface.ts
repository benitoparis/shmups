export interface CropCycles {
  rightCycleLoop: { cropX: number; cropY: number }[];
  leftCycleLoop: { cropX: number; cropY: number }[];
  upCycleLoop: { cropX: number; cropY: number }[];
  downCycleLoop: { cropX: number; cropY: number }[];
}
